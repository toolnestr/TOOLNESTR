---
import ToolLayout from '../../layouts/ToolLayout.astro';
const title = 'Box Weight Calculator';
const description = 'Estimate the weight of a packed box from its dimensions and contents density, and find the dimensional (billable) shipping weight carriers charge on.';
const intro = 'Estimate a packed box’s weight from its size and contents, plus the dimensional weight carriers bill on.';
const category = { id: 'construction', name: 'Construction & Home Improvement', emoji: '🏠' };
const faqs = [
  { q: 'How do I estimate the weight of a box from its size?', a: 'Find the volume in cubic feet — length × width × height in inches, divided by 1728 — then multiply by the contents density in pounds per cubic foot. A 18 × 12 × 12 in box of books (~35 lb/ft³) holds 1.5 ft³ × 35 = about 53 pounds.' },
  { q: 'What is dimensional (DIM) weight?', a: 'Dimensional weight is a pricing weight carriers use for large but light packages. It equals length × width × height in inches divided by a DIM divisor (commonly 139 for domestic air or 166 for international). You are billed on the greater of actual weight and dimensional weight.' },
  { q: 'Why do carriers charge dimensional weight?', a: 'A big, light box takes up truck or plane space that could hold heavier freight. Charging by dimensional weight lets carriers price by the space a parcel occupies, not just its scale weight, so bulky packaging is not shipped cheaply.' },
  { q: 'How can I lower my shipping cost?', a: 'Use the smallest box that safely fits the contents. Reducing any dimension cuts the dimensional weight directly, and a tighter box also uses less void fill. If actual weight already exceeds dimensional weight, a smaller box will not change the billed weight.' },
];
---

<ToolLayout title={title} description={description} intro={intro} category={category} faqs={faqs}>
  <div class="mt-6 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:p-6">
    <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      <label class="block"><span class="text-sm font-medium text-slate-700">Length (in)</span>
        <input id="len" type="number" step="any" placeholder="18" class="mt-1 w-full rounded-xl border border-slate-300 p-3 outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100" /></label>
      <label class="block"><span class="text-sm font-medium text-slate-700">Width (in)</span>
        <input id="width" type="number" step="any" placeholder="12" class="mt-1 w-full rounded-xl border border-slate-300 p-3 outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100" /></label>
      <label class="block"><span class="text-sm font-medium text-slate-700">Height (in)</span>
        <input id="height" type="number" step="any" placeholder="12" class="mt-1 w-full rounded-xl border border-slate-300 p-3 outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100" /></label>
      <label class="block"><span class="text-sm font-medium text-slate-700">Contents</span>
        <select id="density" class="mt-1 w-full rounded-xl border border-slate-300 p-3 outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100">
          <option value="7">Light / packing (~7 lb/ft³)</option>
          <option value="15">Clothing, soft goods (~15 lb/ft³)</option>
          <option value="35" selected>Books, dense goods (~35 lb/ft³)</option>
          <option value="60">Tools, metal parts (~60 lb/ft³)</option>
        </select></label>
      <label class="block"><span class="text-sm font-medium text-slate-700">DIM divisor</span>
        <select id="dim" class="mt-1 w-full rounded-xl border border-slate-300 p-3 outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100">
          <option value="139" selected>139 (domestic air)</option>
          <option value="166">166 (international)</option>
          <option value="194">194 (ground / other)</option>
        </select></label>
    </div>
    <div id="results" class="mt-6 hidden space-y-3">
      <div class="rounded-xl bg-indigo-600 p-5 text-center text-white">
        <div class="text-sm opacity-80">Billable weight</div>
        <div id="r-bill" class="mt-1 text-2xl font-bold">—</div>
        <div id="r-note" class="text-sm opacity-80"></div>
      </div>
      <div class="grid gap-3 sm:grid-cols-3">
        <div class="rounded-xl bg-slate-50 p-3 text-center"><div class="text-sm text-slate-500">Actual weight</div><div id="r-actual" class="mt-1 text-lg font-bold text-slate-800">—</div></div>
        <div class="rounded-xl bg-slate-50 p-3 text-center"><div class="text-sm text-slate-500">Dimensional weight</div><div id="r-dim" class="mt-1 text-lg font-bold text-slate-800">—</div></div>
        <div class="rounded-xl bg-slate-50 p-3 text-center"><div class="text-sm text-slate-500">Volume</div><div id="r-vol" class="mt-1 text-lg font-bold text-slate-800">—</div></div>
      </div>
    </div>
  </div>

  <figure class="mt-10">
    <svg width="800" height="220" viewBox="0 0 800 220" fill="none" xmlns="http://www.w3.org/2000/svg" role="img" aria-labelledby="bw-title bw-desc">
      <title id="bw-title">Box weight formulas</title>
      <desc id="bw-desc">A shipping box with the actual-weight and dimensional-weight formulas</desc>
      <path d="M90 90 L170 90 L170 160 L90 160 Z" fill="#fef3c7" stroke="#d97706" stroke-width="1.5" />
      <path d="M90 90 L110 72 L190 72 L170 90 Z" fill="#fde68a" stroke="#d97706" stroke-width="1.5" />
      <path d="M170 90 L190 72 L190 142 L170 160 Z" fill="#fcd34d" stroke="#d97706" stroke-width="1.5" />
      <text x="140" y="185" font-size="12" fill="#475569" text-anchor="middle" font-family="system-ui,sans-serif">L × W × H</text>
      <rect x="260" y="55" width="480" height="120" rx="8" fill="#f8fafc" stroke="#e2e8f0" stroke-width="1" />
      <text x="280" y="84" font-size="13" font-weight="bold" fill="#1e293b" font-family="system-ui,sans-serif">Two weights</text>
      <text x="280" y="110" font-size="12" fill="#475569" font-family="system-ui,sans-serif">actual = (L&times;W&times;H &divide; 1728) &times; density</text>
      <text x="280" y="132" font-size="12" fill="#475569" font-family="system-ui,sans-serif">dimensional = (L&times;W&times;H) &divide; DIM divisor</text>
      <text x="280" y="154" font-size="12" fill="#475569" font-family="system-ui,sans-serif">billable = the greater of the two</text>
    </svg>
    <figcaption class="mt-2 text-center text-sm text-slate-500">Carriers bill on whichever is larger — the scale weight or the space the box takes.</figcaption>
  </figure>

  <section class="mt-10">
    <h2 class="text-xl font-bold text-slate-900">How the box weight calculator works</h2>
    <p class="mt-2 text-slate-600">
      There are two weights that matter for a packed box. The actual weight is the real weight on a scale: the box's volume in cubic feet — length × width × height in inches divided by 1728 — multiplied by the density of what is inside, in pounds per cubic foot. Light packing material runs under 10 pounds per cubic foot, clothing around 15, books and dense goods near 35, and tools or metal parts 60 or more.
    </p>
    <p class="mt-3 text-slate-600">
      The second is dimensional weight, a pricing weight carriers apply to big but light parcels. It is the cubic-inch volume divided by a DIM divisor set by the carrier and service — commonly 139 for domestic air, 166 for international, and higher numbers for ground. A larger divisor produces a smaller dimensional weight, so the divisor is effectively the space-to-weight exchange rate the carrier uses.
    </p>
    <p class="mt-3 text-slate-600">
      You are charged on the billable weight, which is simply the greater of the actual and dimensional weights. For heavy, compact shipments the actual weight usually wins; for bulky, lightweight ones the dimensional weight does. That is why right-sizing the box matters: trimming any dimension lowers the dimensional weight directly, but only helps your bill when dimensional weight is the one being charged.
    </p>
    <div class="mt-4 grid gap-4 sm:grid-cols-3">
      <div class="rounded-xl border border-slate-200 bg-white p-4 text-center">
        <span class="inline-flex h-10 w-10 items-center justify-center rounded-full bg-indigo-100 text-lg font-bold text-indigo-700">1</span>
        <h3 class="mt-3 font-semibold text-slate-900">Measure the box</h3>
        <p class="mt-1 text-sm text-slate-600">Outside length, width, and height in inches.</p>
      </div>
      <div class="rounded-xl border border-slate-200 bg-white p-4 text-center">
        <span class="inline-flex h-10 w-10 items-center justify-center rounded-full bg-indigo-100 text-lg font-bold text-indigo-700">2</span>
        <h3 class="mt-3 font-semibold text-slate-900">Pick the contents</h3>
        <p class="mt-1 text-sm text-slate-600">Choose a density that matches what's inside.</p>
      </div>
      <div class="rounded-xl border border-slate-200 bg-white p-4 text-center">
        <span class="inline-flex h-10 w-10 items-center justify-center rounded-full bg-indigo-100 text-lg font-bold text-indigo-700">3</span>
        <h3 class="mt-3 font-semibold text-slate-900">Compare weights</h3>
        <p class="mt-1 text-sm text-slate-600">See actual, dimensional, and billable weight.</p>
      </div>
    </div>
  </section>

  <section class="mt-10">
    <h2 class="text-xl font-bold text-slate-900">The formula explained</h2>
    <div class="mt-3 space-y-3">
      <div class="rounded-xl border border-slate-200 bg-white p-4">
        <p class="font-semibold text-slate-900">Actual weight</p>
        <p class="mt-1 text-slate-600">
          <span class="font-mono">= (L&times;W&times;H &divide; 1728) &times; density</span>. An 18 × 12 × 12 in box of books: (2592 ÷ 1728) × 35 = 1.5 × 35 = <strong>52.5 lb</strong>.
        </p>
      </div>
      <div class="rounded-xl border border-slate-200 bg-white p-4">
        <p class="font-semibold text-slate-900">Dimensional weight</p>
        <p class="mt-1 text-slate-600">
          <span class="font-mono">= (L&times;W&times;H) &divide; DIM</span> = 2592 ÷ 139 = <strong>18.6 → 19 lb</strong>.
        </p>
      </div>
      <div class="rounded-xl border border-slate-200 bg-white p-4">
        <p class="font-semibold text-slate-900">Billable weight</p>
        <p class="mt-1 text-slate-600">
          The greater of 52.5 and 19 = <strong>53 lb</strong> billed (dense contents win here).
        </p>
      </div>
    </div>
    <div class="mt-5 rounded-xl border border-slate-200 bg-slate-50 p-5">
      <h3 class="font-semibold text-slate-900">Worked example</h3>
      <p class="mt-1 text-sm text-slate-600">A big, light box of pillows: 24 × 20 × 20 in, ~7 lb/ft³, DIM 139.</p>
      <div class="mt-2 grid gap-1 text-sm sm:grid-cols-2">
        <div><span class="font-medium text-slate-700">Volume:</span> <span class="text-slate-600">9,600 in³ ÷ 1728 = 5.56 ft³</span></div>
        <div><span class="font-medium text-slate-700">Actual:</span> <span class="text-slate-600">5.56 × 7 = 38.9 lb</span></div>
        <div><span class="font-medium text-slate-700">Dimensional:</span> <span class="text-slate-600">9,600 ÷ 139 = 69.1 → 70 lb</span></div>
        <div><span class="font-medium text-slate-700">Billable:</span> <span class="text-slate-600"><strong>70 lb</strong> (dimensional wins)</span></div>
      </div>
    </div>
  </section>

  <section class="mt-10">
    <h2 class="text-xl font-bold text-slate-900">Related calculators</h2>
    <div class="mt-3 flex flex-wrap gap-3">
      <a href="/tools/cubic-yardage-calculator" class="rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm font-medium text-slate-700 transition hover:border-indigo-300 hover:bg-indigo-50">Cubic Yardage Calculator</a>
      <a href="/tools/metal-weight-calculator" class="rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm font-medium text-slate-700 transition hover:border-indigo-300 hover:bg-indigo-50">Metal Weight Calculator</a>
      <a href="/tools/gravel-sand-calculator" class="rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm font-medium text-slate-700 transition hover:border-indigo-300 hover:bg-indigo-50">Gravel &amp; Sand Calculator</a>
    </div>
  </section>

  <script is:inline>
    const el = (id) => document.getElementById(id);

    function update() {
      const L = parseFloat(el('len').value);
      const W = parseFloat(el('width').value);
      const H = parseFloat(el('height').value);
      const density = parseFloat(el('density').value);
      const dim = parseFloat(el('dim').value);
      if (!(L > 0 && W > 0 && H > 0)) { el('results').classList.add('hidden'); return; }

      const cubicIn = L * W * H;
      const cf = cubicIn / 1728;
      const actual = cf * density;
      const dimW = Math.ceil(cubicIn / dim);
      const bill = Math.max(Math.ceil(actual), dimW);

      el('r-bill').textContent = bill + ' lb';
      el('r-note').textContent = (dimW > actual ? 'dimensional weight governs' : 'actual weight governs');
      el('r-actual').textContent = actual.toFixed(1) + ' lb';
      el('r-dim').textContent = dimW + ' lb';
      el('r-vol').textContent = cf.toFixed(2) + ' ft³';
      el('results').classList.remove('hidden');
    }

    ['len', 'width', 'height'].forEach((id) => el(id).addEventListener('input', update));
    ['density', 'dim'].forEach((id) => el(id).addEventListener('change', update));
  </script>
</ToolLayout>
