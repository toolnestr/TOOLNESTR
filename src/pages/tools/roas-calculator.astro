---
import ToolLayout from '../../layouts/ToolLayout.astro';

const title = 'ROAS Calculator';
const description =
  'Free ROAS calculator. Work out your return on ad spend and ACoS from revenue and ad spend. ' +
  'Perfect for Facebook, Google, TikTok and Amazon ads. Any currency.';
const intro =
  'Enter the revenue from your ads and what you spent to get your ROAS (return on ad spend) and ACoS.';
const category = { id: 'finance', name: 'Finance' };
const faqs = [
  { q: 'What is ROAS?', a: 'Return On Ad Spend is the revenue you earn for every unit of currency spent on advertising. ROAS = revenue ÷ ad spend. A ROAS of 4 means you earned 4× what you spent.' },
  { q: 'What is a good ROAS?', a: 'It depends on your margins, but many businesses aim for a ROAS of 3–4 or higher. Lower-margin products need a higher ROAS to be profitable.' },
  { q: 'What is ACoS?', a: 'Advertising Cost of Sale is the inverse — ad spend as a percentage of revenue (spend ÷ revenue × 100). It’s commonly used for Amazon ads.' },
];
---

<ToolLayout title={title} description={description} intro={intro} category={category} faqs={faqs}>
  <div class="mt-6 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:p-6">
    <div class="grid gap-4 sm:grid-cols-3">
      <label class="block"><span class="text-sm font-medium text-slate-700">Currency</span>
        <select id="cur" class="mt-1 w-full rounded-xl border border-slate-300 p-3 outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100"><option>$</option><option>€</option><option>£</option><option>₹</option><option>₨</option><option>¥</option><option>₦</option><option>A$</option></select></label>
      <label class="block"><span class="text-sm font-medium text-slate-700">Revenue from ads</span>
        <input id="rev" type="number" step="any" placeholder="4000" class="mt-1 w-full rounded-xl border border-slate-300 p-3 outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100" /></label>
      <label class="block"><span class="text-sm font-medium text-slate-700">Ad spend</span>
        <input id="spend" type="number" step="any" placeholder="1000" class="mt-1 w-full rounded-xl border border-slate-300 p-3 outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100" /></label>
    </div>

    <div class="mt-6 grid grid-cols-1 gap-3 sm:grid-cols-3">
      <div class="rounded-xl bg-indigo-600 p-5 text-center text-white"><div class="text-sm opacity-80">ROAS</div><div id="r-roas" class="mt-1 text-2xl font-bold">—</div></div>
      <div class="rounded-xl bg-slate-50 p-5 text-center"><div class="text-sm text-slate-500">ACoS</div><div id="r-acos" class="mt-1 text-2xl font-bold text-slate-800">—</div></div>
      <div class="rounded-xl bg-slate-50 p-5 text-center"><div class="text-sm text-slate-500">Profit (rev − spend)</div><div id="r-profit" class="mt-1 text-2xl font-bold text-emerald-600">—</div></div>
    </div>
  </div>

  <section class="mt-10">
    <h2 class="text-xl font-bold text-slate-900">About ROAS</h2>
    <p class="mt-2 text-slate-600">
      Return On Ad Spend is the key metric for judging whether your advertising is working. This
      calculator instantly turns your campaign revenue and spend into ROAS and ACoS, so marketers,
      dropshippers and online sellers can quickly see how profitable their Facebook, Google, TikTok
      or Amazon ads really are.
    </p>
  </section>

  <Fragment slot="related">
    <h2 class="text-xl font-bold text-slate-900">Related tools</h2>
    <p class="mt-2 text-sm text-slate-600">
      Try the <a href="/tools/profit-margin" class="font-medium text-indigo-600 hover:underline">Profit Margin Calculator</a>
      or <a href="/tools/percentage-calculator" class="font-medium text-indigo-600 hover:underline">Percentage Calculator</a>.
    </p>
  </Fragment>

  <script is:inline>
    const el = (id) => document.getElementById(id);
    const cur = el('cur');
    const fmt = (n) => cur.value + n.toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    function update() {
      const rev = parseFloat(el('rev').value);
      const spend = parseFloat(el('spend').value);
      if (isNaN(rev) || isNaN(spend) || spend <= 0) { ['r-roas', 'r-acos', 'r-profit'].forEach((id) => (el(id).textContent = '—')); return; }
      el('r-roas').textContent = (Math.round(rev / spend * 100) / 100) + '×';
      el('r-acos').textContent = (Math.round(spend / rev * 10000) / 100) + '%';
      el('r-profit').textContent = fmt(rev - spend);
    }
    ['cur', 'rev', 'spend'].forEach((id) => el(id).addEventListener('input', update));
  </script>
</ToolLayout>
